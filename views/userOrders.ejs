<!DOCTYPE html>
<html lang="zxx">

<head>
    <title>I WEAR</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <meta name="keywords" content="Goggles a Responsive web template, Bootstrap Web Templates, Flat Web Templates, Android Compatible web template, 
    Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyEricsson, Motorola web design" />
    <script>
        addEventListener("load", function () {
            setTimeout(hideURLbar, 0);
        }, false);

        function hideURLbar() {
            window.scrollTo(0, 1);
        }
    </script>
    <link rel="stylesheet" href="css/owl.carousel.css" type="text/css" media="all">
    <link rel="stylesheet" href="css/owl.theme.css" type="text/css" media="all">
    <link href="css/fontawesome-all.css" rel="stylesheet">
    <link href="//fonts.googleapis.com/css?family=Inconsolata:400,700" rel="stylesheet">
    <link
        href="//fonts.googleapis.com/css?family=Poppins:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/addAdress.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/2.0.1/css/dataTables.bootstrap5.css">
    <script src="js/addressValidation.js"></script>
    <link rel="stylesheet" type="text/css" href="css/style.css">
    <style>
        .hh {
            color: rgb(0, 0, 0);
            text-align: justify;
            margin-left: 20px;
            font-weight: bolder;
        }

        body {
            font-family: 'Poppins', sans-serif;
        }

        .banner-top {
            text-align: center;
            padding: 20px;
        }

        .logo-w3layouts a {
            color: #000;
            text-decoration: none;
        }

        .top-info,
        .top-info-cart {
            padding: 10px;
        }

        .errNoProduct {
            text-align: center;
            margin-top: 50px;
        }

        .footer {
            background-color: #000;
            color: #fff;
            margin-top: 150px;
            position: relative;
        }

        .footer h4 {
            margin-bottom: 20px;
        }

        .footer ul li {
            margin-bottom: 10px;
        }

        .footer ul li a {
            text-decoration: none;
            color: #fff;
        }

        .footer a {
            margin-right: 10px;
        }

        @media (max-width: 768px) {

            .top-info,
            .top-info-cart {
                text-align: center;
            }
        }

        .table {
            width: 100%;
            border-collapse: collapse;
        }

        .table th,
        .table td {
            padding: 8px;
            border: 1px solid #ddd;
        }

        .table-container {
            max-width: 810px;
            margin: 0 auto;
        }
        .bttn{
            border-radius: 7px;
            background-color:#76797a;
            color: transparent;
            color: white;
        }
    </style>
</head>

<body>
    <div class="banner-top container-fluid" id="home">
        <!-- header -->
        <header class="container bg-light">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-md-3 col-6 top-info text-left mt-lg-4">
                        <!-- Add your top info content here -->
                    </div>
                    <div class="col-md-6 col-6 logo-w3layouts text-center">
                        <h1 class="logo-w3layouts">
                            <a class="navbar-brand" href="/">
                                I <i class="ri-eye-fill"></i> WEAR </a>
                        </h1>
                    </div>
                    <div class="col-md-3 col-6 top-info-cart text-right mt-lg-4">
                        <!-- Your cart and login buttons here -->
                        <ul class="cart-inner-info">
                            <li>
                                <a href="/login"><i class="ri-account-circle-fill" style="color: black;"></i></a>
                            </li>
                            <li>
                                <a href="/wishlist"><i class="ri-heart-fill" style="color: black;"></i></a>
                            </li>
                            <li>
                                <a href="/cart"><i class="ri-shopping-cart-line" style="color: black;"></i></a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </header>
        <!-- //header -->
        <br><br>
        <div class="bd">
            <div class="table-container">
                <h1 class="mb-4 hh">Ordered Products</h1>
                <div class="table-responsive">
                    <table class="table table-striped" style="width:100%">
                        <thead>
                            <tr>
                                <th class="thh" scope="col">Product Name</th>
                                <th class="thh" scope="col">Quantity</th>
                                <th class="thh" scope="col">Order Date</th>
                                <th class="thh" scope="col">Price</th>
                                <th class="thh" scope="col">Status</th>
                                <th class="thh" scope="col">Action</th>
                                <th class="thh" scope="col">Order Details</th>
                            </tr>
                        </thead>
                        <tbody>

                            <% for(let i=0; i < orders.length; i++) { %>
                               
                                <tr>
                                    <td>
                                        <%= orders[i].orderItems.Pname %>
                                    </td>
                                    <td>
                                        <%= orders[i].orderItems.units %>
                                    </td>
                                    <td>
                                        <%= orders[i].orderDate.toLocaleString('en-IN').split(',')[0] %>
                                    </td>
                                    <td>
                                        <%= orders[i].orderItems.price%>
                                    </td>
                                    <td>
                                        <% if (orders[i].orderItems.orderStatus=="ordered" ) { %>
                                            <h6>Ordered</h6>
                                            <% } else if (orders[i].orderItems.orderStatus=="Shipped" ) { %>
                                                <h6 class="text-info">Shipped</h6>
                                                <% } else if (orders[i].orderItems.orderStatus=="Delivered" ) { %>
                                                    <h6 class="text-success">Delivered</h6>
                                                    <% } else if (orders[i].orderItems.orderStatus=="Returned" ) { %>
                                                        <h6 class="text-warning">Returned</h6>
                                                        <% }else if (orders[i].orderItems.orderStatus=="Pending" ) { %>
                                                            <h6 class="text-warning">Pending</h6>
                                                            <% } else if (orders[i].orderItems.orderStatus=="Cancelled" ) { %>
                                                                <h6 class="text-warning">Cancelled</h6>
                                                                <% } else { %>
                                                            <h6 class="p-2"></h6>
                                                            <% } %>
                                    </td>
                                    <td>
                                        <% if (orders[i].orderItems.orderStatus=="Delivered" ) { %>
                                            <a href="/invoice?orderId=<%=orders[i]._id %>"><button type="button" class="btn btn-primary btn-sm invoice-btn"
                                                style="background-color:#beb9b988;border: transparent;color: #000;border-radius: 10px;">
                                                <i class="ri-file-3-fill"></i>
                                            </button></a>
                                            <br><br>
                                            <!-- <select name="action" class="form-control">
                                                <option value="return">Return Order</option>
                                            </select> -->
                                            <button type="button" class="btn btn-warning mt-2 btn-sm return-btn"
                                                data-order-id="<%= orders[i]._id %>"
                                                data-product-id="<%= orders[i].orderItems.productId %>">Return</button>
                                            <% } else if (orders[i].orderItems.orderStatus !="Cancelled" &&
                                                orders[i].orderItems.orderStatus !="Returned" && orders[i].orderItems.orderStatus !="Shipped" ) { %>
                                                <button type="button" class="btn btn-sm cancel-btn"
                                                    style="background-color: rgb(178, 0, 0);color: white;"
                                                    data-order-id="<%= orders[i]._id %>"
                                                    data-product-id="<%= orders[i].orderItems.productId %>">
                                                    Cancel Order
                                                </button>
                                                <% } %>
                                    </td>
                                    <td>
                                        <a href="/singleOrder?orderId=<%=orders[i]._id %>&productId=<%=orders[i].orderItems.productId %>"><button type="button" class="bttn">Details</button></a>
                                    </td>
                                </tr>
                                <% } %>
                        </tbody>
                    </table>
                </div>
                <% if (orders.length===0) { %>
                <div class="errNoProduct position-absolute">
                    <h1 style="color: #dbf800;">You didn't order anything!</h1>
                    <h4 class="my-6"><a href="/" class="btn btn-warning">Order Now</a></h4>
                </div>
                <% } %>
            </div>
            <div class="pagination-area mt-15 mb-50">
                    <nav aria-label="Page navigation example">
                        <ul id="pagination" class="pagination justify-content-center">
                            <!-- Pagination buttons will be generated here dynamically -->
                        </ul>
                    </nav>
                </div>
           
        </div>
        <br><br>
    </div>
    </div>
    <footer class="footer footer-fixed bg-dark">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h4 class="text-light">Quick Links</h4>
                    <ul class="list-unstyled">
                        <li><a class="text-light" href="#">Home</a></li>
                        <li><a class="text-light" href="#">About</a></li>
                        <li><a class="text-light" href="#">Shop</a></li>
                        <li><a class="text-light" href="#">Contact Us</a></li>
                    </ul>
                </div>
                <div class="col-md-6 text-md-right">
                    <h4 class="text-light">Follow Us</h4>
                    <a href="#" style="color: deeppink;"><i class="ri-instagram-line"></i></a>
                    <a class="text-light" href="#"><i class="ri-twitter-x-line"></i></a>
                    <a class="text-primary" href="#"><i class="ri-facebook-circle-fill"></i></a>
                </div>
            </div>
        </div>
    </footer>
    <!-- //footer -->
    <script src="js/jquery-2.2.3.min.js"></script>
    <script src="js/simplyCountdown.js"></script>
    <link href="css/simplyCountdown.css" rel='stylesheet' type='text/css' />
    <script src="js/owl.carousel.js"></script>
    <script src="js/minicart.js"></script>
    <script src="js/move-top.js"></script>
    <script src="js/easing.js"></script>
    <script src="js/modernizr-2.6.2.min.js"></script>
    <script src="js/classie-search.js"></script>
    <script src="js/demo1-search.js"></script>


    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"
        integrity="sha512-v2CJ7UaYy4JwqLDIrZUI/4hqeoQieOmAZNXBeQyjo21dadnwR+8ZaIJVT8EE2iyI61OV8e6M8PP2/4hpQINQ/g=="
        crossorigin="anonymous" referrerpolicy="no-referrer"></script>
        <script>
        $(document).ready(function () {
            var itemsPerPage = 4; // Number of items per page
            var currentPage = 1; // Current page
            var totalItems = '<%= orders.length %>'; // Total number of items
            var totalPages = Math.ceil(totalItems / itemsPerPage); // Total number of pages
    
            // Function to display items for the current page
            function showPage(page) {
                $('tbody tr').hide(); // Hide all table rows
                var startIndex = (page - 1) * itemsPerPage; // Calculate starting index
                var endIndex = Math.min(startIndex + itemsPerPage, totalItems); // Calculate ending index
                $('tbody tr').slice(startIndex, endIndex).show(); // Show items for the current page
            }
    
            // Function to generate pagination buttons
            function generatePaginationButtons() {
                var paginationHtml = '';
                for (var i = 1; i <= totalPages; i++) {
                    paginationHtml += '<li class="page-item' + (i === currentPage ? ' active' : '') + '"><a class="page-link" href="#" onclick="changePage(' + i + ')">' + i + '</a></li>';
                }
                $('#pagination').html(paginationHtml); // Update pagination buttons
            }
    
            // Function to handle page change
            window.changePage = function (page) {
                currentPage = page; // Update current page
                showPage(currentPage); // Show items for the new page
                generatePaginationButtons(); // Regenerate pagination buttons
            };
    
            showPage(currentPage); // Show items for the initial page
            generatePaginationButtons(); // Generate pagination buttons
        });
    </script>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    

    <script>
        $(document).ready(function () {
            // AJAX for cancelling order
            $('.cancel-btn').click(function () {
                var orderId = $(this).data('order-id');
                var productId = $(this).data('product-id');
                $.ajax({
                    url: '/cancelOrder',
                    method: 'GET',
                    data: { orderId: orderId, productId: productId },
                    success: function (response) {
                        // Handle success response
                        if (response.success) {
                            console.log(response.message); // Log success message
                            window.location.reload()
                        } else {
                            console.error(response.error); // Log error message
                            // Optionally, perform any other action you want upon error
                        }
                    },
                    error: function (xhr, status, error) {
                        // Handle error
                        console.error(error); // Log the error
                        // Optionally, perform any other action you want upon error
                    }
                });
            });


            // AJAX for returning order
            $('.return-btn').click(function () {
                var orderId = $(this).data('order-id');
                var productId = $(this).data('product-id');
                $.ajax({
                    url: '/returnOrder',
                    method: 'GET',
                    data: { orderId: orderId, productId: productId },
                    success: function (response) {
                        // Handle success response
                        if (response.success) {
                            console.log(response.message); // Log success message
                            window.location.reload()
                        } else {
                            console.error(response.error); // Log error message
                            // Optionally, perform any other action you want upon error
                        }
                    },
                    error: function (xhr, status, error) {
                        // Handle error
                        console.error(error); // Log the error
                        // Optionally, perform any other action you want upon error
                    }
                });
            });

        });
    </script>

</body>

</html>